// Command server is Stocker's entrypoint. It wires the workers, the boss,
// and the HTTP edge together and starts the server.
package main

func main() {
	// TODO(step 1): construct the stub overview worker, hand it to the
	// boss, mount the httpapi handler, and start listening.
}
