// Package report defines Stocker's shared output vocabulary: the assembled
// Report, its Sections, and per-section status. It is a leaf package — it
// depends on nothing, so every other package may depend on it.
package report
